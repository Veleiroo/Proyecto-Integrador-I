
To try the baseline model follow the following steps:

1. Run the executable:
python baselines\roboflow\roboflow_baseline.py

1. Wait for the camera window to open. When it opens, click on it. The system will only respond if the last thing that was clicked is the camera window.

2. Follow the instructions in the terminal ("c" for capturing an image, "q" for quitting).